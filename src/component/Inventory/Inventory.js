import React from 'react';

const Inventory = () => {
    const styles = {textAlign: 'center'}
    return (
        <div>
            <h1 style={styles}>Manage Inventory Comming soon...</h1>
        </div>
    );
};

export default Inventory;